# Copy to .env beside this file. Only the drizzle-kit scripts read it.
# `db:generate` never connects, but still needs every variable set.
DB_HOST=localhost
DB_USER=propsim
DB_PASS=propsim
DB_NAME=propsim
