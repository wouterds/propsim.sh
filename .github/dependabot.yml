version: 2

# Without a prefix Dependabot writes "⬆️ Bump x from a to b". `scope` appends
# (deps) / (deps-dev). Every ecosystem uses chore, so the log reads one way.
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
      include: "scope"

  # Compose pins the image the server runs.
  - package-ecosystem: "docker-compose"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
      include: "scope"

  # Each app pins its own base image, so each one has to be listed.
  - package-ecosystem: "docker"
    directories:
      - "/apps/web"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
      include: "scope"

  # The root holds the toolchain only. A workspace with runtime dependencies
  # has to be listed.
  - package-ecosystem: "npm"
    directories:
      - "/"
      - "/apps/web"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: "scope"
